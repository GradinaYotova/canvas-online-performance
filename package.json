{
  "name": "draw-canvas",
  "version": "1.0.0",
  "description": "real-time drawing canvas",
  "main": "server.js",
  "scripts": {
    "start": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gradina",
  "license": "ISC",
  "dependencies": {
    "express": "^*",
    "socket.io": "*"
  }
  "engines":{
    "node": "12.10.0"
  }
}
